{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @portfolio/server build && if [ -n \"$DATABASE_URL\" ]; then pnpm --filter @portfolio/server db:migrate; else echo 'No DATABASE_URL — skipping migrations'; fi && pnpm --filter @portfolio/server print-schema && pnpm --filter @portfolio/web run relay && pnpm --filter @portfolio/web build",
  "outputDirectory": "packages/web/dist",
  "rewrites": [{ "source": "/((?!api/).*)", "destination": "/index.html" }]
}
